# 电脑工作规则

> 两台电脑协作，避免混乱

---

## 🆔 电脑代号

| 电脑 | 代号 | PAIOS 位置 |
|------|------|------------|
| 新电脑 | **evan** | E:\PAIOS |
| 老电脑 | **feng** | F:\PAIOS |

---

## 🔄 推拉顺序（铁律）

```
evan ──push──▶ GitHub ──pull──▶ feng
      ◀──pull──            ◀──push──
```

### 标准流程

```
① 开始工作前  →  git pull          ← 先拉取，避免冲突
② 修改代码    →  git add + commit  
③ 工作完成    →  git push          ← 推送上线
④ 换电脑时    → 先 pull → 再继续    ← 到另一台电脑先拉取
```

### ⚠ 绝对禁止

```
✗ 不 pull 就直接 push          → 会被拒绝（rejected）
✗ 两台电脑同时修改同一文件      → 合并冲突
✗ push 完不通知直接关机        → 另一台 pull 时可能意外
```

### 冲突处理

如果 `git push` 被拒绝：
```
git pull --rebase    ← 拉取并变基
# 解决冲突（如果有）
git push             ← 推送
```

---

## 🖥 首次设置（每台电脑只需一次）

### 新电脑（evan）— 已配置好

### 老电脑（feng）

```bash
# 1. 克隆/拉取仓库
cd /f/
git clone <仓库地址> PAIOS
# 或 cd /f/PAIOS && git pull

# 2. 运行一键配置脚本
bash scripts/setup-computer.sh feng

# 3. 重新打开 Git Bash
```

---

## 💡 提示

- 每次 `git checkout` / `git pull` 会自动显示当前电脑代号和操作顺序提醒
- 终端提示符进入 paios 目录时会显示 `【新电脑 · evan】` 或 `【老电脑 · feng】`
- 不确定时：**先 pull，再问**

---

## 🌐 环境变量 `PAIOS_DRIVE`（核心机制）

仓库里所有路径不再写死盘符，而是使用 `$PAIOS_DRIVE` 环境变量。

| 电脑 | 代号 | 盘符 | `~/.bashrc` 自动设置 |
|------|------|------|---------------------|
| 新电脑 | evan | **E** | `export PAIOS_DRIVE=E` |
| 老电脑 | feng | **F** | `export PAIOS_DRIVE=F` |

### 涉及的文件

- `30_SYSTEM/Config/paths.yaml` — 使用 `${PAIOS_DRIVE}`
- `40_AUTOMATION/05_SCRIPTS/classify_files.py` — 读取 `PAIOS_DRIVE`
- 其他脚本和文档 — 引用 `%PAIOS_DRIVE%` 说明

### 验证

```bash
echo $PAIOS_DRIVE    # 应显示 E 或 F
```
