# 照片整理工具 · 环境变量配置（本地密钥，请勿提交）
#
# 用法：复制本文件为 .env，填入真实值：
#   cp .env.example .env
#   （然后编辑 .env，把下面的值换成你的真实 Key）
#
# 说明：
#   - 后台 / 非交互 shell 不会继承你在交互终端里 export 的变量，
#     因此把密钥写进 .env 是最稳妥的做法（脚本启动时自动读取）。
#   - .env 仅本地使用，含密钥，切勿提交到任何版本库。
#   - 值两端可加引号，脚本会自动去除。

# 智谱 GLM-4V-Flash（多标签补判引擎，CLIP 置信度 < 0.40 时触发）
# 申请：https://open.bigmodel.cn/  → 创建 API Key（glm-4v-flash 为免费模型）
ZHIPU_API_KEY=你的智谱APIKey

# 百度图像识别（备用分类引擎，默认未启用）
# BAIDU_API_KEY=你的百度APIKey
# BAIDU_SECRET_KEY=你的百度SecretKey
