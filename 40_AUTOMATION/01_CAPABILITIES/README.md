# 01_CAPABILITIES — Atomic Capabilities

> **Purpose**: Reusable, composable capability definitions
> **Contains**: One subdirectory per capability (summarize/, review/, etc.)
> **Rules**: Each capability has capability.md + metadata.yaml
> **Constraint**: No disk path references — only type-based sources
