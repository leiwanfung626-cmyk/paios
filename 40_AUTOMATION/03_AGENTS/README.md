# 03_AGENTS — Agent Definitions

> **Purpose**: Agent = capability combination + prompt + provider
> **Contains**: One YAML per agent (workbuddy.yaml, daily_review.yaml, etc.)
> **Format**: identity + capabilities (by ID) + workflow + provider
