# 10_MANIFEST — Automation Platform Metadata

> ⚠️ **DEPRECATED（Phase B, 2026-07-11）**：本目录为旧 Manifest 系统，已被 `collect_manifest.py`（→ `PAIOS-Usage/manifest.yaml` → publish → `F:\Fleet\incoming\`）取代。保留至 Step 4 物理隔离时删除，**请勿在新流程中使用**。

> **Purpose**: Platform version, asset counts, migration status
> **Contains**:
>   manifest.yaml           — platform metadata (version, schema)
>   migration_status.yaml   — migration progress tracking
>   inventory.yaml          — asset snapshot (auto-generated)
